# Today-I-Learned

English: Today I worked on developing my task reminder backend application while adding the login feature to the application several errors came up and I had to fix them one by one since the whole app wouldn’t work properly if the login route wasn’t implemented correctly I spent the entire day focusing on creating the login route and solving the related issues I checked error messages reorganized the code structure, and analyzed why the problems were happening which took longer than I expected In the end I managed to fix all the errors and the login route is now working properly user authentication is now functioning as expected and I can successfully obtain the token needed to actually create task reminders however due to time constraints I haven’t tested whether the task creation feature itself is working yet I plan to check that tomorrow and make additional fixes if necessary

Korean: 오늘 나는 할 일 알림 백엔드 애플리케이션을 개발하면서 작업을 진행했다 애플리케이션에 로그인 기능을 추가하는 과정에서 여러 가지 오류가 발생했고 그 오류들을 하나씩 수정해야 했다 로그인 라우트를 제대로 구현하지 않으면 전체 기능이 정상적으로 동작하지 않아서 하루 종일 로그인 라우트를 만드는 것과 관련된 문제들을 해결하는 데 집중했다 에러 메시지를 확인하고 코드 구조를 다시 정리하면서 왜 문제가 발생하는지 분석했고 그 과정이 생각보다 오래 걸렸다 하지만 결국 모든 오류를 하나씩 해결하는 데 성공했고 로그인 라우트가 정상적으로 작동하게 되었다 이제 사용자 인증이 제대로 이루어지고 할 일 알림을 실제로 생성하는 데 필요한 토큰도 문제없이 받아올 수 있다 다만 오늘은 시간 관계상 할 일 생성 기능 자체가 제대로 작동하는지는 아직 테스트하지 못했다 그 부분은 내일 다시 확인하고 문제가 있다면 추가로 수정할 계획이다
